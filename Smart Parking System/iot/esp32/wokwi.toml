[wokwi]
version = 1
elf = ".pio/build/wokwi/firmware.elf"
firmware = ".pio/build/wokwi/firmware.bin"

# ESP32 simulated Wi-Fi — Wokwi provides internet access in simulation mode
# WiFi credentials "Wokwi-GUEST" / "" are defined in config.h under #ifndef PRODUCTION_MODE
[net]
enabled = true
